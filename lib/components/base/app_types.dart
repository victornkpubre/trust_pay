enum TransactionType {moneyPool, secureSales, groupGoals, billSplitter, betsAndWager}
enum TransactionStatus {pending, accepted, declined, verification, completed}
enum ObligationStatus {pending, fulfilled, paid, verified}

